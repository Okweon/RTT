# tdm_generator.py dependencies
# Install:  pip install -r requirements_tdm_generator.txt
#
# Verified with Python 3.11 and: pandas 2.2.3 / nltk 3.9.1 /
# scikit-learn 1.5.2

pandas>=1.5           # input table and output matrices
nltk>=3.8             # POS tagging, lemmatization, English stop words
scikit-learn>=1.2     # CountVectorizer

# The first run downloads three NLTK resources automatically:
#   wordnet, stopwords, averaged_perceptron_tagger_eng
# On an offline machine, fetch them in advance with:
#   python -c "import nltk; [nltk.download(r) for r in ('wordnet','stopwords','averaged_perceptron_tagger_eng')]"
